"""
Top-level package for the Flask Transportation Management System application.

This file exists to ensure `App` is treated as a proper Python package so imports
like `from App.Website.LandingPage import landing` work reliably regardless of
the current working directory (e.g., when launching from the repo root).
"""
